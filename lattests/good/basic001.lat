/* void expression as statement */

int main() {
  int[] a = new int[3];
  f(a);
  printInt(a[0]);
  printInt(a[1]);
  printInt(a[2]);
  printInt(a.length);
  return 0 ;
}

void f(int[] x) {
	int a = 0;
	while(a < x.length) {
		x[a] = a * 111;
		a = a + 1;
	}
}